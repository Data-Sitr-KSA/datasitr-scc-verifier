{
  "verdict": "FAIL",
  "v0_1_expected": "FAIL",
  "v0_2_expected": "FAIL",
  "notes": "Purely Layer 1 failure: scc_id does not match the required pattern, parties.importer is missing, and every mandatory top-level clause after 'parties' is absent. This vector catches a regression in JSON Schema validation and is the simplest end-to-end negative test. Unlike the semantic-failure vectors, this one fails at Layer 1 — Layer 2 is skipped (NOT_APPLICABLE) because the document is structurally broken, which is the intended behavior: Rego rules that dereference missing fields would raise."
}
